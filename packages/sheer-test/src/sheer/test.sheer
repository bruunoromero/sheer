(ns sheer.test)

(defn describe [desc f]
  ((from-global :describe) desc (discard f)))

(defn it [desc f]
  ((from-global :it) desc (discard f)))

(defn expect [v]
  ((from-global :expect) v))

(defn same? [v exp]
  (.toBe ((from-global :expect) v) exp))
